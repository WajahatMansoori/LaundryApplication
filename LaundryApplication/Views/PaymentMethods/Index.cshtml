@model IEnumerable<LaundryApplication.Models.PaymentMethod>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutBEnd.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pay_Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pay_Des)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pay_Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pay_Des)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pay_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pay_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pay_Id })
        </td>
    </tr>
}

</table>
